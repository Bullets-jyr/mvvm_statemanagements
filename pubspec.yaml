name: mvvm_statemanagements
description: "mvvm statemanagements"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.4.1
  get_it: ^8.0.2
  flutter_dotenv: ^5.2.1
  shared_preferences: ^2.3.3
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/.env